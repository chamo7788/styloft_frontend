// Main index file for RequestForm components

// Import only the component that exists
import GarmentRequestForm from './GarmentRequestForm';

// Export the component
export {
  GarmentRequestForm
};

// Default export
export default GarmentRequestForm;